<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>모바일 청첩장 - 12월의 겨울 웨딩</title>
    <style>
        /* 기본 스타일 세팅 */
        :root {
            --bg-color: #f7f9fa;
            --main-color: #2c3e50;
            --accent-color: #8b0000; /* 겨울 포인트 컬러 (딥 레드/버건디) */
            --text-color: #333333;
            --card-bg: #ffffff;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: 'Pretendard', 'Noto Sans KR', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            text-align: center;
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* 눈 내리는 애니메이션 Canvas */
        #snow-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 9999;
        }

        .container {
            max-width: 480px;
            margin: 0 auto;
            background-color: var(--card-bg);
            box-shadow: 0 4px 20px rgba(0,0,0,0.05);
            min-height: 100vh;
        }

        /* 헤더 / 메인 커버 */
        .header {
            padding: 50px 20px;
            background: linear-gradient(180deg, #eef2f5 0%, #ffffff 100%);
        }

        .sub-title {
            font-size: 14px;
            letter-spacing: 2px;
            color: #777;
            margin-bottom: 10px;
        }

        .main-title {
            font-size: 24px;
            font-weight: 600;
            color: var(--main-color);
            margin-bottom: 30px;
        }

        .main-img {
            width: 100%;
            height: 350px;
            background-color: #eee;
            border-radius: 8px;
            object-fit: cover;
        }

        /* 섹션 공통 */
        .section {
            padding: 40px 20px;
            border-bottom: 1px solid #f0f0f0;
        }

        .section-title {
            font-size: 18px;
            font-weight: 600;
            color: var(--main-color);
            margin-bottom: 20px;
            letter-spacing: 1px;
        }

        /* 인사말 */
        .greeting-text {
            font-size: 15px;
            color: #555;
            word-break: keep-all;
        }

        /* D-Day 카운트다운 */
        .dday-box {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 20px;
        }

        .dday-item {
            background: #f8f9fa;
            padding: 10px 15px;
            border-radius: 8px;
            min-width: 50px;
        }

        .dday-number {
            font-size: 20px;
            font-weight: bold;
            color: var(--main-color);
        }

        .dday-label {
            font-size: 12px;
            color: #888;
        }

        /* 버튼 스타일 */
        .btn {
            display: inline-block;
            padding: 12px 24px;
            margin: 5px;
            border-radius: 6px;
            background-color: #f1f3f5;
            color: #333;
            text-decoration: none;
            font-size: 14px;
            border: none;
            cursor: pointer;
            transition: background-color 0.2s;
        }

        .btn-primary {
            background-color: var(--main-color);
            color: #fff;
        }

        /* 계좌 안내 모달/토글 */
        .account-box {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 15px;
            margin-top: 10px;
            text-align: left;
        }

        .account-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 8px;
            font-size: 14px;
        }

        /* 푸터 */
        .footer {
            padding: 30px 20px;
            font-size: 12px;
            color: #aaa;
        }
    </style>
</head>
<body>

    <!-- 눈 내리는 효과 Canvas -->
    <canvas id="snow-canvas"></canvas>

    <div class="container">
        <!-- 메인 헤더 -->
        <header class="header">
            <div class="sub-title">WINTER WEDDING</div>
            <div class="main-title">신랑 이름 & 신부 이름</div>
            <img src="https://via.placeholder.com/400x500?text=Winter+Wedding+Photo" alt="메인 사진" class="main-img">
            <p style="margin-top: 20px; font-size: 15px; color: #666;">
                2026년 12월 19일 토요일 오후 1시<br>
                OO웨딩홀 O층 OO홀
            </p>
        </header>

        <!-- 인사말 섹션 -->
        <section class="section">
            <div class="section-title">INVITATION</div>
            <div class="greeting-text">
                하얀 눈이 포근하게 세상을 덮는 12월,<br>
                저희 두 사람이 서로의 따뜻한 온기가 되어<br>
                평생을 함께하려 합니다.<br><br>
                차가운 계절의 길목에서 따뜻한 마음으로<br>
                첫걸음을 내딛는 날, 고마운 분들을 모시고<br>
                축복을 나누고자 합니다.<br><br>
                부디 오셔서 저희의 시작을 밝게 비춰주세요.
            </div>
        </section>

        <!-- D-Day 섹션 -->
        <section class="section">
            <div class="section-title">COUNTDOWN</div>
            <div id="dday-text" style="font-weight: bold; margin-bottom: 10px;"></div>
            <div class="dday-box">
                <div class="dday-item"><div class="dday-number" id="days">0</div><div class="dday-label">DAYS</div></div>
                <div class="dday-item"><div class="dday-number" id="hours">0</div><div class="dday-label">HOURS</div></div>
                <div class="dday-item"><div class="dday-number" id="minutes">0</div><div class="dday-label">MINS</div></div>
                <div class="dday-item"><div class="dday-number" id="seconds">0</div><div class="dday-label">SECS</div></div>
            </div>
        </section>

        <!-- 오시는 길 -->
        <section class="section">
            <div class="section-title">LOCATION</div>
            <p><strong>OO웨딩홀</strong></p>
            <p style="font-size: 14px; color: #666;">서울시 OO구 OO로 123 (전화번호)</p>
            <div style="margin-top: 15px;">
                <a href="https://map.kakao.com" target="_blank" class="btn btn-primary">카카오맵 연결</a>
                <a href="https://navi.kakao.com" target="_blank" class="btn">내비게이션</a>
            </div>
        </section>

        <!-- 마음 전하실 곳 -->
        <section class="section">
            <div class="section-title">ACCOUNT</div>
            <p style="font-size: 13px; color: #777; margin-bottom: 15px;">참석이 어려우신 분들을 위해 마음 전하실 곳을 남깁니다.</p>
            
            <div class="account-box">
                <div class="account-row">
                    <span><strong>신랑측 계좌</strong> (OO은행 123-456-78901)</span>
                    <button class="btn" style="padding: 4px 8px; font-size: 12px;" onclick="copyAccount('12345678901')">복사</button>
                </div>
                <div class="account-row">
                    <span><strong>신부측 계좌</strong> (OO은행 987-654-32109)</span>
                    <button class="btn" style="padding: 4px 8px; font-size: 12px;" onclick="copyAccount('98765432109')">복사</button>
                </div>
            </div>
        </section>

        <footer class="footer">
            Thank you for celebrating with us.
        </footer>
    </div>

    <script>
        // 1. 눈 내리는 애니메이션 (Snow Effect)
        const canvas = document.getElementById('snow-canvas');
        const ctx = canvas.getContext('2d');

        function resizeCanvas() {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
        }
        window.addEventListener('resize', resizeCanvas);
        resizeCanvas();

        const flakes = [];
        for (let i = 0; i < 50; i++) {
            flakes.push({
                x: Math.random() * canvas.width,
                y: Math.random() * canvas.height,
                radius: Math.random() * 3 + 1,
                density: Math.random() * 1,
                opacity: Math.random() * 0.7 + 0.3
            });
        }

        function drawSnow() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            ctx.fillStyle = 'rgba(255, 255, 255, 0.8)';
            ctx.beginPath();
            for (let i = 0; i < flakes.length; i++) {
                const f = flakes[i];
                ctx.moveTo(f.x, f.y);
                ctx.arc(f.x, f.y, f.radius, 0, Math.PI * 2, true);
            }
            ctx.fill();
            updateSnow();
        }

        function updateSnow() {
            for (let i = 0; i < flakes.length; i++) {
                const f = flakes[i];
                f.y += Math.cos(f.density) + 1 + f.radius / 2;
                f.x += Math.sin(f.density) * 0.5;

                if (f.y > canvas.height) {
                    flakes[i] = { x: Math.random() * canvas.width, y: 0, radius: f.radius, density: f.density };
                }
            }
        }

        setInterval(drawSnow, 30);

        // 2. D-Day 카운트다운 (2026년 12월 19일 기준)
        const weddingDate = new Date("2026-12-19T13:00:00").getTime();

        function updateDday() {
            const now = new Date().getTime();
            const distance = weddingDate - now;

            if (distance < 0) {
                document.getElementById("dday-text").innerHTML = "예식이 시작되었습니다.";
                return;
            }

            const days = Math.floor(distance / (1000 * 60 * 60 * 24));
            const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1000);

            document.getElementById("days").innerText = days;
            document.getElementById("hours").innerText = hours;
            document.getElementById("minutes").innerText = minutes;
            document.getElementById("seconds").innerText = seconds;
            document.getElementById("dday-text").innerText = `예식까지 ${days}일 남았습니다.`;
        }

        setInterval(updateDday, 1000);
        updateDday();

        // 3. 계좌번호 복사 기능
        function copyAccount(accountNumber) {
            navigator.clipboard.writeText(accountNumber).then(() => {
                alert("계좌번호가 복사되었습니다: " + accountNumber);
            });
        }
    </script>
</body>
</html>